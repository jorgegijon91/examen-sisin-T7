
Vagrant.configure("2") do |config|
  
  #Configuración de la box de ubuntu
  config.vm.box = "ubuntu/xenial64"
  

  

  
   #config.vm.provision "shell", inline: <<-SHELL
    #sudo apt-get update
    #echo "-- Insertar datos de ejemplo en la tabla 'menu'" > /home/vagrant/datos_menu.sql
   # echo "INSERT INTO datos_menu.menu() VALUES" >> /home/vagrant/datos_menu.sql
   # echo "(Valores de los campos de la tabla separados por coma)," >> /home/vagrant/datos_menu.sql   #SHIFT ALT CURSO ABAJO-->Duplico
   #SHELL
end
